---
- name: Installiere Apache auf dem Webserver
  hosts: webserver
  become: yes
  tasks:
    - name: Aktualisiere APT-Paketliste
      apt:
        update_cache: yes
    - name: Installiere Apache
      apt:
        name: apache2
        state: present
    - name: Starte Apache
      service:
        name: apache2
        state: started
